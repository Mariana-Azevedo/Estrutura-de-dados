#include <stdio.h>

/* No quick sort vc escolhe um pivó e dps faz dois for um comparando se tal número daquele vetor é maior ou menos que o pivó*/

int main()
{
    int tam, i, j, pivo;
    int vetor[100], aux [100];

    scanf("%d", &tam);
    scanf("%d", &vetor);

    for(i=1;i<tam;i++)
    {
        pivo = vetor[0];
        if (vetor[i] < pivo)
        {

        }
        /*for (j = 0; vetor[0] < i; j++)
        {
            vetor[0 + j];
        }*/
        

    }
}



